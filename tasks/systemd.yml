---
- name: Ensure systemd php directory exists
  ansible.builtin.file:
    group: root
    mode: "0755"
    owner: root
    path: /etc/systemd/system/php{{ php_version }}-fpm.service.d
    state: directory

- name: Ensure override.conf is installed
  ansible.builtin.template:
    dest: /etc/systemd/system/php{{ php_version }}-fpm.service.d/override.conf
    group: root
    mode: "0644"
    owner: root
    src: etc/systemd/system/override.conf.j2
  notify:
    - Reload systemd daemon
    - Reload php-fpm
